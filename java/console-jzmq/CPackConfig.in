set(CPACK_DEBIAN_PACKAGE_ARCHITECTURE all)
set(CPACK_RPM_PACKAGE_ARCHITECTURE    noarch)
set(CPACK_DEBIAN_PACKAGE_SECTION net)
set(CPACK_DEBIAN_PACKAGE_DEPENDS "cameo-server (>=2.0.0)")
set(CPACK_RPM_PACKAGE_REQUIRES "cameo-server >= 2.0.0")

set(CPACK_DEBIAN_PACKAGE_PROVIDES "cameo-console (=${PROJECT_VERSION})")
set(CPACK_RPM_PACKAGE_PROVIDES "cameo-console")
