set(CPACK_DEBIAN_PACKAGE_ARCHITECTURE all)
set(CPACK_RPM_PACKAGE_ARCHITECTURE    noarch)
set(CPACK_DEBIAN_PACKAGE_SECTION net)
set(CPACK_DEBIAN_PACKAGE_DEPENDS "libzmq-jni, cameo-proxy(>=2.0.0)")
set(CPACK_RPM_PACKAGE_REQUIRES "zeromq")
#set(CPACK_DEBIAN_PACKAGE_SUGGESTS "java9-sdk")

set(CPACK_DEBIAN_PACKAGE_PROVIDES "cameo-server (=${PROJECT_VERSION})")
set(CPACK_RPM_PACKAGE_PROVIDES "cameo-server")